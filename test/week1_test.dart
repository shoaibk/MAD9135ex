import 'package:week1/week1.dart';
import 'package:test/test.dart';

// nothing changed here since the test is not in the test directory yet (it is in the test directory already) so we just create the test directory and

void main() {
  test('calculate', () {
    expect(calculate(), 42);
  });
}
